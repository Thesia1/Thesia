[project]
name = "thesia-forex-bot"
version = "0.1.0"
description = "Live-capable agentic forex trading bot with deterministic strategy and strict risk controls."
requires-python = ">=3.11"
dependencies = [
  "certifi>=2024.0.0",
]

[tool.setuptools.packages.find]
include = ["forex_bot*"]
